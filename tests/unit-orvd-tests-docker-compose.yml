
services:
#################################################
  supa:
    image: orvd
    build:
      context: .
      dockerfile: ./orvd.Dockerfile
    container_name: supa
    command: /var/www/orvd/run_tests.sh
    working_dir: /var/www/orvd
